import styled from "styled-components";

export const Wrap = styled.div`
    font-size: 14px;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.6);
    cursor: pointer;
    &:hover {
        color: rgba(1, 132, 207, 1)
  }
`